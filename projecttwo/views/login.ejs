<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Login</title>

<style>
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

*{margin:0;padding:0;box-sizing:border-box;font-family:"Poppins",sans-serif;}

body{
  height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
  background:#0b001a;
  overflow:hidden;
}

/* Neon Glow animation */
.glow-box{
  width:380px;
  backdrop-filter:blur(18px);
  border:2px solid rgba(153,0,255,0.6);
  box-shadow:0 0 20px rgba(208,0,255,0.85);
  padding:45px;
  border-radius:18px;
  animation:pop .9s ease-out;
}
@keyframes pop{
  from{transform:scale(0.8);opacity:0;}
  to{transform:scale(1);opacity:1;}
}

h2{
  text-align:center;
  color:#d400ff;
  margin-bottom:25px;
  letter-spacing:1px;
}

input{
  width:100%;
  padding:12px;
  margin:12px 0;
  background:#1c0030;
  border:none;
  border-left:3px solid #b300ff;
  border-radius:6px;
  color:#fff;
  font-size:16px;
  outline:none;
}
input:focus{
  box-shadow:0 0 12px #c300ff;
}

button{
  width:100%;
  padding:12px;
  margin-top:15px;
  background:#a700ff;
  border:none;
  color:white;
  font-size:18px;
  border-radius:6px;
  cursor:pointer;
  transition:.3s;
}
button:hover{
  background:#d800ff;
  box-shadow:0 0 15px #e600ff;
}

p{
  text-align:center;
  margin-top:15px;
  color:white;
}
a{
  color:#ff4dff;
  text-decoration:none;
}
a:hover{text-shadow:0 0 10px #ff5afc;}

.energy{
  position:absolute;
  width:100%;
  height:3px;
  background:linear-gradient(90deg,#8900ff,#ff00e6,#8900ff);
  top:0;
  animation:move 4s linear infinite;
}
@keyframes move{
  0%{transform:translateX(-100%);}
  100%{transform:translateX(100%);}
}
</style>
</head>

<body>

<div class="glow-box">
  <div class="energy"></div>
  <h2>Cyber Login</h2>
  <form action="/loginUser">
    <input type="text" name="username" placeholder="Username">
    <input type="password" name="password" placeholder="Password">
    <button>Login</button>
  </form>
  <p>New User? <a href="/signUp">Create Account</a></p>
</div>

</body>
</html>
